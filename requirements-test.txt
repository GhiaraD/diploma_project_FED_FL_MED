# Dependențe pentru rularea testelor locale (fără GPU)
# Instalare: pip install -r requirements-test.txt

numpy>=2.0
scikit-learn>=1.8
hypothesis>=6.0
pytest>=9.0
pytest-cov>=7.0
torch>=2.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.20 --index-url https://download.pytorch.org/whl/cpu
tqdm>=4.0
Pillow>=10.0
cryptography>=42.0
opencv-python-headless>=4.0
